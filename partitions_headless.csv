# Headless console partition table (4 MB flash). Two OTA app slots so the portal's
# OTA route works, each ~1.9 MB — comfortably larger than the ~1 MB networking build,
# fixing both the single-app "no OTA partition" limitation and the flash pressure.
# Name,     Type, SubType,  Offset,    Size
nvs,        data, nvs,      0x9000,    0x6000
otadata,    data, ota,      0xf000,    0x2000
phy_init,   data, phy,      0x11000,   0x1000
ota_0,      app,  ota_0,    0x20000,   0x1D0000
ota_1,      app,  ota_1,    0x1F0000,  0x1D0000
